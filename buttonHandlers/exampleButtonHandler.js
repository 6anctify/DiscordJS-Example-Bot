const { EmbedBuilder, PermissionsBitField } = require('discord.js');

module.exports = {
    customId: 'yourCustomID', //Specified custom id when constructing the button
    async execute(interaction) {
        console.log('Success');
    }
};
